<%= form_for(publico_produto) do |f| %>
  <% if publico_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publico_produto.errors.count, "error") %> prohibited this publico_produto from being saved:</h2>

      <ul>
      <% publico_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_area :descricao %>
  </div>
  
  <div class="field">
    <%= f.label :fotos %>
    <%= f.file_field :fotos, multiple: true %>
  </div>
  


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
