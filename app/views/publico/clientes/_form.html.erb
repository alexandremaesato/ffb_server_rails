<%= form_for(publico_cliente) do |f| %>
  <% if publico_cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publico_cliente.errors.count, "error") %> prohibited this publico_cliente from being saved:</h2>

      <ul>
      <% publico_cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_area :descricao %>
  </div>

  <div class="field">
    <%= f.label :aniversario %>
    <%= f.date_select :aniversario %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
