<%= form_for(publico_ambiente) do |f| %>
  <% if publico_ambiente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publico_ambiente.errors.count, "error") %> prohibited this publico_ambiente from being saved:</h2>

      <ul>
      <% publico_ambiente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_area :descricao %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
