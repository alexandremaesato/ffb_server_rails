<%= form_for(favorito) do |f| %>
  <% if favorito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorito.errors.count, "error") %> prohibited this favorito from being saved:</h2>

      <ul>
      <% favorito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cliente_id %>
    <%= f.number_field :cliente_id %>
  </div>

  <div class="field">
    <%= f.label :favoritado_id %>
    <%= f.number_field :favoritado_id %>
  </div>

  <div class="field">
    <%= f.label :tipo_favoritado %>
    <%= f.number_field :tipo_favoritado %>
  </div>

  <div class="field">
    <%= f.label :checked %>
    <%= f.check_box :checked %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
